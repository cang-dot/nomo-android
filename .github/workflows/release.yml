name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows x64
            runner: windows-2022
            rust-target: ''
            args: '--bundles nsis'
          - name: macOS arm64
            runner: macos-latest
            rust-target: aarch64-apple-darwin
            args: '--target aarch64-apple-darwin'

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Setup pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.5.1 --activate

      - name: Setup Rust
        if: matrix.rust-target == ''
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust target
        if: matrix.rust-target != ''
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Clean stale Windows release outputs
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          @(
            'src-tauri\target\release\bundle',
            'src-tauri\target\release\nsis',
            'src-tauri\target\release\wix'
          ) | ForEach-Object {
            Remove-Item -LiteralPath $_ -Recurse -Force -ErrorAction SilentlyContinue
          }

      - name: Build macOS Quick Look extension
        if: runner.os == 'macOS'
        shell: bash
        env:
          QUICKLOOK_ARCH: ${{ matrix.rust-target == 'aarch64-apple-darwin' && 'arm64' || 'x86_64' }}
          APPLE_CODESIGN_IDENTITY: '-'
        run: pnpm run build:quicklook-extension

      - name: Prepare release body
        id: release_body
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $notesFile = ".github/release-notes/${{ github.ref_name }}.md"
          if (Test-Path -LiteralPath $notesFile) {
            $body = Get-Content -LiteralPath $notesFile -Raw
          } else {
            $body = @(
              'Windows:',
              '- x64-setup.exe: 安装版',
              '- x64.zip: 免安装版',
              '- checksums.md5: MD5 校验清单',
              '',
              'macOS:',
              '- .dmg / .app.tar.gz: arm64'
            ) -join "`n"
          }

          'body<<__RELEASE_BODY__' >> $env:GITHUB_OUTPUT
          $body.TrimEnd() >> $env:GITHUB_OUTPUT
          '__RELEASE_BODY__' >> $env:GITHUB_OUTPUT

      - name: Build and publish release assets
        uses: tauri-apps/tauri-action@v0.6.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPLE_SIGNING_IDENTITY: '-'
        with:
          tagName: ${{ github.ref_name }}
          releaseName: Nomo ${{ github.ref_name }}
          releaseBody: ${{ steps.release_body.outputs.body }}
          releaseDraft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          args: ${{ matrix.args }}

      - name: Package Windows zip
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $version = '${{ github.ref_name }}' -replace '^v', ''
          $zipRoot = Join-Path $env:RUNNER_TEMP 'nomo-x64'
          $appDir = Join-Path $zipRoot 'Nomo'
          $zipPath = Join-Path $env:GITHUB_WORKSPACE "Nomo_${version}_x64.zip"

          Remove-Item -LiteralPath $zipRoot -Recurse -Force -ErrorAction SilentlyContinue
          Remove-Item -LiteralPath $zipPath -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Path $appDir | Out-Null

          Copy-Item -LiteralPath 'src-tauri\target\release\nomo.exe' -Destination (Join-Path $appDir 'nomo.exe')
          if (Test-Path -LiteralPath 'src-tauri\target\release\resources') {
            Copy-Item -LiteralPath 'src-tauri\target\release\resources' -Destination (Join-Path $appDir 'resources') -Recurse
          }

          Compress-Archive -LiteralPath $appDir -DestinationPath $zipPath -Force
          "NOMO_ZIP_PATH=$zipPath" >> $env:GITHUB_ENV

      - name: Upload Windows zip release asset
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload '${{ github.ref_name }}' "$env:NOMO_ZIP_PATH" --clobber

  checksums:
    name: Publish MD5 checksums
    runs-on: ubuntu-latest
    needs: build
    if: ${{ !contains(github.ref_name, '-') }}

    steps:
      - name: Generate and upload checksums.md5
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-assets
          gh release download '${{ github.ref_name }}' --dir release-assets --clobber
          rm -f release-assets/checksums.md5 checksums.md5

          mapfile -d '' files < <(find release-assets -maxdepth 1 -type f ! -name 'checksums.md5' -print0 | sort -z)
          : > checksums.md5
          for file in "${files[@]}"; do
            md5="$(md5sum "$file" | awk '{print $1}')"
            name="$(basename "$file")"
            printf '%s  %s\n' "$md5" "$name" >> checksums.md5
          done

          gh release upload '${{ github.ref_name }}' checksums.md5 --clobber

  update-homebrew-cask:
    name: Update Homebrew cask
    runs-on: ubuntu-latest
    needs: checksums
    if: ${{ !contains(github.ref_name, '-') }}
    permissions:
      contents: write

    steps:
      - name: Checkout default branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.repository.default_branch }}

      - name: Bump Casks/nomo.rb
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          version="${TAG#v}"
          asset="Nomo_${version}_aarch64.dmg"
          gh release download "$TAG" --pattern "$asset" --dir /tmp
          sha256="$(sha256sum "/tmp/$asset" | awk '{print $1}')"
          sed -i -E \
            -e "s/version \"[^\"]+\"/version \"${version}\"/" \
            -e "s/sha256 \"[^\"]+\"/sha256 \"${sha256}\"/" \
            Casks/nomo.rb
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Casks/nomo.rb
          if git diff --staged --quiet; then
            echo 'Homebrew cask already up to date.'
            exit 0
          fi
          git commit -m "chore(homebrew): bump nomo cask to ${version}"
          git push

  sync-pages:
    name: Sync release to download sites
    runs-on: ubuntu-latest
    needs: [build, checksums]
    if: >-
      ${{
        always() &&
        needs.build.result == 'success' &&
        (contains(github.ref_name, '-') || needs.checksums.result == 'success')
      }}
    timeout-minutes: 30

    steps:
      - name: Trigger Pages deployment
        env:
          DEPLOY_HOOK_URL: ${{ secrets.CLOUDFLARE_PAGES_DEPLOY_HOOK }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$DEPLOY_HOOK_URL" || {
            echo 'Missing CLOUDFLARE_PAGES_DEPLOY_HOOK repository secret.' >&2
            exit 1
          }
          curl --fail --show-error --silent --request POST "$DEPLOY_HOOK_URL" >/dev/null

      - name: Trigger GitHub Pages deployment
        env:
          GH_TOKEN: ${{ secrets.WEBSITE_ACTIONS_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$GH_TOKEN" || {
            echo 'Missing WEBSITE_ACTIONS_TOKEN repository secret.' >&2
            exit 1
          }
          gh api \
            --method POST \
            repos/nomo-md/nomo-md.github.io/actions/workflows/deploy-github-pages.yml/dispatches \
            --field ref=master

      - name: Verify latest stable download mirror
        if: ${{ !contains(github.ref_name, '-') }}
        env:
          EXPECTED_TAG: ${{ github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          base_urls=(
            'https://nomo-md.pages.dev'
            'https://nomo-md.github.io'
          )
          declare -A manifests=()

          for attempt in $(seq 1 150); do
            all_ready=true
            for base_url in "${base_urls[@]}"; do
              if [[ -n "${manifests[$base_url]:-}" ]]; then
                continue
              fi

              if manifest="$(curl --fail --silent --show-error \
                "$base_url/downloads/latest.json?expected=$EXPECTED_TAG&attempt=$attempt")"; then
                actual_tag="$(jq -r '.tag // empty' <<<"$manifest")"
                asset_count="$(jq -r '.assets | length' <<<"$manifest")"
                if [[ "$actual_tag" == "$EXPECTED_TAG" && "$asset_count" == '4' ]]; then
                  manifests[$base_url]="$manifest"
                  continue
                fi
              fi

              all_ready=false
            done

            if [[ "$all_ready" == 'true' && "${#manifests[@]}" == "${#base_urls[@]}" ]]; then
              break
            fi
            sleep 10
          done

          [[ "${#manifests[@]}" == "${#base_urls[@]}" ]] || {
            echo "The download sites did not publish $EXPECTED_TAG within 25 minutes." >&2
            exit 1
          }

          for base_url in "${base_urls[@]}"; do
            mapfile -t asset_urls < <(jq -r '.assets[].url' <<<"${manifests[$base_url]}")
            [[ "${#asset_urls[@]}" == '4' ]]
            for asset_url in "${asset_urls[@]}"; do
              curl --fail --silent --show-error --location --head "$base_url$asset_url" >/dev/null
            done
          done
